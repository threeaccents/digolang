let a = 1;
let b = 1;

let sum = fn(a, b) {a+b};

let result = sum(5, 5);

let max = fn(x, y) {
    if(x > y) {
        return x;
    }

    return y;
};

let no_value;

if isNull(no_value) {
    return "is null!"
}

let greeter = fn(name) {
    return "hello" + " " + name
}

greeter("digolang")

let whichIsMax = max(10, 20);

return whichIsMax;

